"""Bootstrap resampling for covariance inference (Bayesian / classical, optional clusters).

Re-exports: use ``get_joint_bootstrapped_distribution`` from ``kanly.api`` for joint
covariance across already-bootstrapped fits. Full workflow (``do_bootstrap2``, weight
generators) lives in :mod:`kanly.bootstrap.bootstrap` — see ``README.md`` here.
"""
